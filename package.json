{
  "name": "hatnet-demo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "ai-review:dry-run": "node scripts/github-review-bot/review-pr.mjs --dry-run --fixture scripts/github-review-bot/fixtures/small-diff.json",
    "ai-review:self-test": "node scripts/github-review-bot/review-pr.mjs --self-test",
    "dev:local": "docker compose up -d postgres && pnpm db:migrate && pnpm dev",
    "dev": "pnpm --parallel --filter @hatnet/api --filter @hatnet/web dev",
    "dev:api": "pnpm --filter @hatnet/api dev",
    "dev:web": "pnpm --filter @hatnet/web dev",
    "db:migrate": "pnpm --filter @hatnet/api db:migrate",
    "db:revert": "pnpm --filter @hatnet/api db:revert",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}
