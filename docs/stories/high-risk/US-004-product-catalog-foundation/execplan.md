# Execution Plan

1. Add `products` TypeORM entity, repository, service, controller, and module.
2. Add migration that creates and seeds deterministic MVP product rows.
3. Wire the product module/entity/migration into the API app and TypeORM config.
4. Update the React shell to fetch and render the catalog.
5. Add focused tests for product service/controller and frontend rendering.
6. Run migration, API proof, typecheck, tests, build, and Harness trace.
