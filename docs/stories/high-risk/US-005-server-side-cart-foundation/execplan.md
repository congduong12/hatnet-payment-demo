# Execution Plan

1. Add cart entities, repository, service, controller, and module.
2. Add migration for `carts` and `cart_items`.
3. Wire cart into TypeORM config and `AppModule`.
4. Extend product repository with active product lookup by id.
5. Add React signed-in add-to-cart controls and lightweight cart state.
6. Add tests and run migration/runtime proof.
